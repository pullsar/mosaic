name: play_engine
description: Deterministic pure-Dart Mosaic Play state machine.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: '>=3.12.0 <4.0.0'
dependencies:
  play_schema: ^0.1.0
dev_dependencies:
  test: ^1.25.15
