name: event_delivery
description: Cross-platform Mosaic analytics envelope queueing and delivery runtime.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: '>=3.12.0 <4.0.0'
dependencies:
  analytics_contract: ^0.1.0
  http: ^1.6.0
  local_state: ^0.1.0
  platform_contracts: ^0.1.0
  web: ^1.1.1
dev_dependencies:
  test: ^1.31.0
