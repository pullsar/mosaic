name: local_state
description: Durable local identity, preferences, drafts, resume state and event outbox for Mosaic.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: '>=3.12.0 <4.0.0'
dependencies:
  analytics_contract: ^0.1.0
  sqlite3: 3.5.2
dev_dependencies:
  test: ^1.31.0
